# Modul: demo/docguard.config.json

<!-- change: signature-changed old="$schema():" new="$schema():string" -->
### variable: $schema
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `$schema: string`
```ts
$schema: string
```

<!-- change: symbol-added name="generation" kind="variable" -->
### variable: exclude
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `exclude: object`
```ts
exclude: object
```

<!-- change: symbol-added name="include" kind="variable" -->
### variable: generation
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `generation: object`
```ts
generation: object
```

<!-- change: symbol-added name="output" kind="variable" -->
### variable: include
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `include: object`
```ts
include: object
```

<!-- change: symbol-added name="validation" kind="variable" -->
### variable: output
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `output: object`
```ts
output: object
```

### variable: validation
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `validation: object`
```ts
validation: object
```

# Modul: packages/doc-system-agent/examples/basic-project/src/calculator.ts

<!-- change: symbol-added name="power" kind="function" -->
### function: add
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `export add(a: number, b: number): number`
```ts
export add(a: number, b: number): number
```

Parameter:

| Name | Typ | Optional | Default |
|------|-----|----------|---------|
| `a` | `number` | nein | nein |
| `b` | `number` | nein | nein |

Rückgabewert: `number`

<!-- change: symbol-added name="subtract" kind="function" -->
### function: divide
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `export divide(a: number, b: number): number`
```ts
export divide(a: number, b: number): number
```

Parameter:

| Name | Typ | Optional | Default |
|------|-----|----------|---------|
| `a` | `number` | nein | nein |
| `b` | `number` | nein | nein |

Rückgabewert: `number`

### function: multiply
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `export multiply(a: number, b: number): number`
```ts
export multiply(a: number, b: number): number
```

Parameter:

| Name | Typ | Optional | Default |
|------|-----|----------|---------|
| `a` | `number` | nein | nein |
| `b` | `number` | nein | nein |

Rückgabewert: `number`

### function: power
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `export power(base: number, exponent: number): number`
```ts
export power(base: number, exponent: number): number
```

Parameter:

| Name | Typ | Optional | Default |
|------|-----|----------|---------|
| `base` | `number` | nein | nein |
| `exponent` | `number` | nein | nein |

Rückgabewert: `number`

### function: subtract
Rolle: other (Sichtbarkeit: public, Priorität: normal)
Signatur: `export subtract(a: number, b: number): number`
```ts
export subtract(a: number, b: number): number
```

Parameter:

| Name | Typ | Optional | Default |
|------|-----|----------|---------|
| `a` | `number` | nein | nein |
| `b` | `number` | nein | nein |

Rückgabewert: `number`

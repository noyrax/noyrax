# src/calculator.ts

Simple calculator module demonstrating DocGuard documentation.

## Overview

| Property | Value |
|----------|-------|
| **Module** | calculator |
| **File** | `src/calculator.ts` |
| **Language** | TypeScript |
| **Last Updated** | 2024-12-01 |

## Exports

### Classes

#### `Calculator`

A simple calculator class with basic arithmetic operations.

**Example:**
```typescript
const calc = new Calculator();
const result = calc.add(5, 3);
console.log(result.value); // 8
```

##### Methods

| Method | Signature | Description |
|--------|-----------|-------------|
| `add` | `(a: number, b: number) => CalculationResult` | Adds two numbers |
| `subtract` | `(a: number, b: number) => CalculationResult` | Subtracts the second number from the first |
| `multiply` | `(a: number, b: number) => CalculationResult` | Multiplies two numbers |
| `divide` | `(a: number, b: number) => CalculationResult` | Divides the first number by the second |
| `getHistory` | `() => CalculationResult[]` | Returns the calculation history |
| `clearHistory` | `() => void` | Clears the calculation history |

### Functions

#### `factorial`

```typescript
function factorial(n: number): number
```

Calculates the factorial of a number.

**Parameters:**
- `n` - The number to calculate factorial for (must be >= 0)

**Returns:** The factorial of n

**Throws:** Error if n is negative

**Example:**
```typescript
factorial(5); // 120
factorial(0); // 1
```

---

#### `fibonacci`

```typescript
function fibonacci(n: number): number
```

Calculates the nth Fibonacci number.

**Parameters:**
- `n` - The position in the Fibonacci sequence (0-indexed)

**Returns:** The nth Fibonacci number

**Example:**
```typescript
fibonacci(0); // 0
fibonacci(1); // 1
fibonacci(10); // 55
```

## Dependencies

### Imports

| Source | Symbols |
|--------|---------|
| `./types` | `CalculationResult` (type) |

### Dependents

*No dependents detected.*

---

*Generated by [Noyrax](https://noyrax.dev)*


# src/types.ts

Common type definitions for the demo application.

## Overview

| Property | Value |
|----------|-------|
| **Module** | types |
| **File** | `src/types.ts` |
| **Language** | TypeScript |
| **Last Updated** | 2024-12-01 |

## Exports

### Interfaces

#### `User`

Represents a user in the system.

| Property | Type | Description |
|----------|------|-------------|
| `id` | `string` | Unique identifier |
| `name` | `string` | User's display name |
| `email` | `string` | Email address |
| `createdAt` | `Date` | Account creation timestamp |
| `isActive` | `boolean` | Whether the user is active |

---

#### `UserQueryOptions`

Options for user queries.

| Property | Type | Description |
|----------|------|-------------|
| `includeInactive?` | `boolean` | Include inactive users |
| `limit?` | `number` | Maximum number of results |
| `offset?` | `number` | Result offset for pagination |

---

#### `CalculationResult`

Result of a calculation operation.

| Property | Type | Description |
|----------|------|-------------|
| `value` | `number` | The computed value |
| `operation` | `'add' \| 'subtract' \| 'multiply' \| 'divide'` | The operation that was performed |
| `operands` | `[number, number]` | Input operands |
| `timestamp` | `Date` | Timestamp of calculation |

### Types

#### `AppError`

Error types for the application.

```typescript
type AppError = 
  | { type: 'not_found'; message: string }
  | { type: 'validation'; message: string; field: string }
  | { type: 'internal'; message: string };
```

## Dependencies

### Imports

*No imports.*

### Dependents

| File | Symbols Used |
|------|--------------|
| `src/calculator.ts` | `CalculationResult` |
| `src/user-service.ts` | `User`, `UserQueryOptions`, `AppError` |

---

*Generated by [Noyrax](https://noyrax.dev)*


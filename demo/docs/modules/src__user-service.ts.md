# src/user-service.ts

User service module for managing users.

<!-- change: signature-changed old="getUserById(id: string)" new="getUserById(id: string, options?: UserQueryOptions)" -->

## Overview

| Property | Value |
|----------|-------|
| **Module** | user-service |
| **File** | `src/user-service.ts` |
| **Language** | TypeScript |
| **Last Updated** | 2024-12-01 |
| **Status** | ⚠️ Drift Detected |

## Validation Issues

> **⚠️ Drift Warning:** The following signatures have changed since last documentation update:
> 
> - `getUserById`: Expected `(id: string)` but found `(id: string, options?: UserQueryOptions)`
> 
> Run `noyrax generate` to update documentation.

## Exports

### Classes

#### `UserService`

Service for managing user operations.

**Example:**
```typescript
const service = new UserService();
const user = await service.getUserById('123');
```

##### Methods

| Method | Signature | Description |
|--------|-----------|-------------|
| `createUser` | `(name: string, email: string) => User` | Creates a new user |
| `getUserById` | `(id: string, options?: UserQueryOptions) => User \| AppError` | Retrieves a user by their ID |
| `listUsers` | `(options?: UserQueryOptions) => User[]` | Lists all users |
| `updateUser` | `(id: string, updates: Partial<Pick<User, 'name' \| 'email'>>) => User \| AppError` | Updates a user's information |
| `deactivateUser` | `(id: string) => true \| AppError` | Deactivates a user (soft delete) |

### Functions

#### `isValidEmail`

```typescript
function isValidEmail(email: string): boolean
```

Validates an email address format.

**Parameters:**
- `email` - The email to validate

**Returns:** True if valid, false otherwise

---

#### `formatUserDisplay`

```typescript
function formatUserDisplay(user: User): string
```

Formats a user's display name.

**Parameters:**
- `user` - The user object

**Returns:** Formatted display string

## Dependencies

### Imports

| Source | Symbols |
|--------|---------|
| `./types` | `User`, `UserQueryOptions`, `AppError` (types) |

### Dependents

*No dependents detected.*

---

*Generated by [Noyrax](https://noyrax.dev)*

